{
  "name": "docs-crawler-backend",
  "version": "0.1.0",
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.4",
    "tsx": "^4.19.2",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --minify",
    "dev": "bun --hot run src/index.ts"
,
    "lint": "eslint . --ext .ts",
    "test": "bun test"
  },
  "dependencies": {
    "@types/archiver": "^6.0.3",
    "@vercel/postgres": "^0.10.0",
    "archiver": "^7.0.1",
    "bun": "^1.2.1",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.39.1",
    "elysia": "^1.2.10"
  }
}